# leego-design-UI · Web 固定应用框架

```text
Design Standard: leego-design-UI
Standard ID: leego-design-ui
Version: 2.12.0
```

本参考用于带常驻导航的企业 Web 工作台。营销页、连续阅读文档、Mobile App 和 Pad 专用作业台不使用本框架。

## 1. 固定尺寸

- 桌面展开侧栏固定为 `216px`，折叠图标栏固定为 `68px`；移动抽屉独立保持 `248px`。侧栏不得根据菜单文字、权限数量、页面类型或内容宽度自动伸缩，也不提供拖拽改宽。
- 黑色侧栏使用 `#10161D`，占满应用工作区高度。品牌区、用户区和折叠操作固定；只有菜单列表可在短视口内独立滚动。
- 菜单项最小高度 `40px`，导航图标 `20px`，标题使用 14/20px、500–600 字重并保持单行。
- 标准菜单标题不超过 8 个中文字符。超长标题使用省略号，不换行、不撑宽，并通过悬浮、键盘聚焦和无障碍名称提供完整内容。

## 2. 内容边距与顶部节奏

- 全局顶栏高度固定 `64px`，内容左边距 `28px`、右边距 `16px`，形成“左侧建立层级、右侧释放空间”的非对称紧凑布局。
- 顶栏标题、搜索或页面上下文与内容首模块使用同一条 `28px` 左对齐线；页面不得分别硬编码多套左边距。
- 顶栏到首个状态、筛选、任务或业务卡片的距离固定 `12px`；常规模块之间的垂直间距固定 `12px`，页面底部保留 `24px`。
- 固定顶栏已承担页面身份时，内容区不得重复英文眉题、大标题和用途介绍。对象身份、风险结论、数据新鲜度和必要操作说明继续保留。
- Mobile、Pad、营销页和连续阅读页面使用各自的布局令牌，不继承这组 Web 工作台内容边距。

## 3. 固定工作顶栏

- 结构顺序固定为 `业务面包屑 → 全局搜索 → 通知 → 用户身份 → 退出`。左侧只放业务面包屑；产品 Logo 和产品名保留在侧栏品牌区。
- 顶栏高 `64px`，使用白色不透明背景和 1px 中性底边线，不使用渐变、玻璃效果或额外阴影；主内容区独立滚动，顶栏不参与页面滚动。
- 面包屑采用 14/20px：历史层级为中性灰，当前页为 600 字重深色，分隔图标 14px。宽屏显示最近三级，中等桌面显示最近两级，紧凑桌面只显示当前页；视觉截断时仍保留完整无障碍路径和悬浮提示。
- 全局搜索高 `40px`，宽度 `clamp(240px, 24vw, 360px)`，支持 `⌘K / Ctrl+K`；搜索语义必须是跨模块全局搜索，页面内条件搜索继续留在内容区并明确对象范围。
- 通知、退出和纯图标操作视觉尺寸 `40px`、图标 `18px`；触摸环境命中区至少 `44px`。头像 `34px`，姓名 13/20px，角色 12/18px。
- 通知数量来自真实状态，使用红点或 `99+` 角标。搜索、通知、账号或退出能力不存在时隐藏入口或在审计报告中记录缺口，不得伪造数据、空按钮或后台接口。
- 区域、周期、业务警报、状态指标、创建、导出等页面操作进入首个内容工具栏，不进入全局顶栏；不得出现第二个全局搜索入口。

顶栏响应状态：

| 视口 | 面包屑 | 搜索 | 用户区 | 其他 |
| --- | --- | --- | --- | --- |
| `≥1200px` | 最近三级 | 完整搜索框 | 头像、姓名、角色 | 通知、退出 |
| `1024–1199px` | 最近两级 | 完整搜索框 | 仅头像 | 通知、退出 |
| `761–1023px` | 当前页 | 搜索图标 | 仅头像 | 通知、退出 |
| `≤760px` | 独立 Mobile App Bar | 移入移动入口 | 移入抽屉或账号页 | 不复用桌面顶栏 |

## 4. 四态响应式

| 视口 | 默认状态 | 展开方式 | 持久化 |
| --- | --- | --- | --- |
| `≥1024px` | 用户偏好的 216px 或 68px | 交界按钮切换 | 使用应用命名空间的本地键保存 |
| `761–1023px` | 68px | 216px 临时覆盖层 | 不覆盖宽屏偏好 |
| `≤760px` | 关闭 | 248px 移动抽屉 | 不读取桌面偏好 |

- 紧凑覆盖层通过遮罩、`Esc`、完成导航或再次点击折叠按钮关闭；焦点返回触发入口。
- 从紧凑窗口返回宽屏时恢复之前保存的宽屏状态。自动响应式变化不得写入本地存储。
- 移动抽屉由顶栏菜单按钮打开，隐藏黑白交界折叠按钮，并保留安全的遮罩与焦点管理。

## 5. 固定折叠按钮

- 视觉尺寸 `22×42px`，白色背景、1px 中性边框、10px 圆角和轻阴影；位于黑白边界垂直中点，向内容区偏移 `11px`。
- 左右箭头使用 `16px`，展开态指向左，折叠态指向右。图标不可使用文本字符代替。
- 实际命中区域至少 `44×44px`，扩展热区不得改变视觉尺寸或影响相邻内容。
- 按钮提供 `aria-expanded`、`aria-controls` 和“展开导航栏/折叠导航栏”名称；键盘焦点环不能被侧栏或主内容裁切。
- 宽度与按钮位移动画统一为 `220ms`。`prefers-reduced-motion: reduce` 时立即切换，不播放过渡。

## 6. 状态与实现边界

- 使用显式状态区分 `expanded`、`collapsed`、`compact-overlay` 和 `mobile-drawer`，不得只靠多个互相覆盖的 CSS 媒体查询推断状态。
- 本地存储键必须带应用命名空间，例如 `<app-id>:ui:sidebar`；不同系统不得共用无命名空间的全局键。
- 侧栏状态只影响应用外壳，不改变路由、权限过滤、导航分组、业务数据和页面滚动位置。
- 主内容区是页面级唯一纵向滚动容器；侧栏菜单只在自身溢出时滚动。不得形成 `body`、主内容和侧栏菜单三个同时滚动的页面。
- 折叠态纯图标菜单必须显示可见提示和可访问名称；当前选中、禁用和权限隐藏状态不能只用颜色区分。

## 7. 审计与验收

- P0：侧栏宽度或响应模式错误、上下文丢失、重复全局搜索、伪造通知或空按钮、紧凑顶栏溢出、移动端仍使用桌面外壳、状态持久化错误、折叠按钮不可访问或滚动后消失。
- P1：顶栏高度、区域顺序、左右内容边距、首模块间距、按钮尺寸、圆角、图标、菜单或面包屑字号、行高、提示或动画与令牌不一致。
- 至少验证 1440、1024、1023、960、761、760、430×932、390×844 和浏览器 200% 缩放。
- 使用 1–8 个中文字、超长中文、中英混排和动态权限菜单检查不换行、不撑宽、完整提示与键盘操作。

## 8. 反例

- 为容纳长标题把侧栏扩展到 260–320px。
- 顶栏使用 20px 左边距、内容使用 24px 或 32px，导致页面对齐线漂移。
- 为追求“呼吸感”把首模块下推 24–40px，或在右侧保留大面积无功能留白。
- 不同页面使用不同折叠宽度、按钮位置或箭头样式。
- 把问候语、产品名、大标题、区域筛选或创建按钮塞进全局顶栏。
- 页面内搜索和全局搜索没有语义区分，或为缺失的通知、退出能力制造无行为图标。
- 紧凑视口自动写入“折叠”偏好，导致用户回到宽屏后仍丢失原选择。
- 折叠按钮放进可滚动菜单，滚动后无法操作。
- 视觉按钮只有 22px 宽，同时把可点击区域也限制为 22px。
