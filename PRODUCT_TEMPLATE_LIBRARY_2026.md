# 2026 小字号 UI 2.0 · 跨系统优质模板研究

```text
Design Standard: 2026 小字号 UI 2.0
Standard ID: small-type-ui-2026
Version: 2.5.1
```

## 研究目的

本研究从小红花、小金矿、小点位、小太阳、小总管和小葫芦的当前本地界面中提炼可复用的信息结构、交互闭环和响应式方法。来源系统只用于追溯设计经验；模板不复制业务代码、接口、权限、数据或行业规则。

机器可读目录见 `design-system/template-patterns.json`。在线预览使用同一目录，Skill 在审计与实施时也从该目录选择模板。

## 提炼原则

1. 提炼“用户要完成的判断或行动”，不提炼具体业务名词。
2. 保留被真实页面验证过的信息顺序、状态表达和交互关系，不复制视觉噱头。
3. 同一模板必须说明必要数据与失败状态，避免只有理想态截图。
4. Web、Mobile、Pad 分别设计；响应式不是等比缩放。
5. 来源系统的领域规则只留在来源系统，不能通过模板进入其他产品。
6. 已有固定顶部和当前页面标识时，不重复展示页面介绍；模板首屏从状态、筛选、任务或简易可视化开始。

## 来源系统中的可复用经验

### 小红花

- 管理首页将指标、组织概况、人员台账和右侧行动区组合成紧凑工作台。
- 每日闭环台账能区分“没有发生”与“没有填写”，并保留补录和历史证据。
- 成长路径把当前、目标、分组要求、差距和规则版本放在同一解释链中。
- 阶段看板与对象档案联动，流程变化不脱离证据和历史。

不可泛化：招聘阶段名称、绩效口径、积分与晋升规则。

### 小金矿

- 诊断页使用“对象列表—诊断主区—行动侧栏”，适合连续排查和派发动作。
- 经营摘要强调范围、数据新鲜度、来源质量与指标下钻。
- 移动对象卡只保留主指标、进度、少量次级指标和异常结论。

不可泛化：门店健康算法、食安否决和具体经营权重。

### 小点位

- 台账首屏同时显示完整度、来源、负责人和重复数据等治理信息。
- 多方案比较限制选择数量，显式展示样本、口径、准备度和决策条件。
- 建议复核队列区分可直接处理、存在歧义和错误记录。

不可泛化：点位投资模型、地理审批和具体测算公式。

### 小太阳

- 多来源指标看板将筛选写入 URL，返回后保持上下文。
- 指标同时展示覆盖率、基线、单位、来源和更新时间。
- 数据证据复核按字段展示候选值、冲突、置信度和原始证据，批准后追加记录而非覆盖历史。

不可泛化：平台账号层级、内容指标口径和渠道归因规则。

### 小总管

- 首页先展示今日行动和风险，再提供资源指标。
- 合同和资产类页面采用单一工具栏、紧凑统计带、台账与对象 360° 概览。
- 全局命令搜索按对象类型分组，并为无结果提供恢复动作。
- 折叠导航在桌面与移动信息架构之间保持清晰边界。

不可泛化：合同状态、付款口径、资产分类和提醒规则。

### 小葫芦

- 资源台账明确数据来源、快照时间、影子/正式模式和异常金额排除规则。
- 预警中心将严重度摘要、队列、证据、外部任务和关闭依据连成闭环。
- Pad 作业台支持本地草稿、分步骤输入、扫码、在线最终校验、同步冲突和主管审核。

不可泛化：库存成本算法、生产 BOM、批次 FEFO 和正式过账规则。

## 模板选择方法

先判断五个问题：用户要“看结论、查对象、做诊断、作决策还是完成作业”；同时处理多少对象；任务频率；是否需要证据或审核；设备与主要输入方式。再从目录选择一个主模板，必要时组合一个支持模板。

- 管理者先决定今天做什么：`action-command-dashboard`。
- 多来源指标需要解释覆盖率和基线：`multi-source-metric-dashboard`。
- 连续排查多个对象：`diagnostic-triage-workbench`。
- 台账同时承担主数据治理：`governed-entity-registry`。
- 队列式风险或审批：`risk-queue-master-detail` 或 `tablet-approval-master-detail`。
- 复杂现场录入：`tablet-offline-step-workbench` 或 `tablet-high-frequency-entry-console`。
- 移动 AI 助手、智能体或对话工具：`mobile-conversational-agent-canvas`。

不要因为来源系统相似就选择模板；以用户任务和必要数据为准。

## 组合边界

- 一个页面只保留一个主视觉任务。行动驾驶舱可以包含台账摘要，但不能把完整台账嵌入首页。
- 风险队列可以跳转证据复核，但两套工作台不在同一屏并排堆叠。
- 对象 360° 可以引用成长路径或资源明细，但必须通过明确分区或下钻进入。
- Mobile 只承接随时查看、快速处理和证据提交；复杂比较矩阵与大批量治理回到 Web 或 Pad。
- 对话智能体以连续会话为主画布，低频目的地进入侧栏；过程默认折叠，但工作空间、来源、数据新鲜度和审批身份不得隐藏。
- Pad 在窗口不足时优先收起支持区，不能缩小触控目标和正文。

## 审计输出要求

`UI_2_AUDIT.md` 必须增加“模板映射”章节，至少记录：当前页面、用户目标、推荐模板 ID、适配理由、保留的现有组件、不可照搬部分、迁移优先级和验收方法。没有足够信息时可以标记“待确认”，不得用来源系统的业务规则补全未知需求。
