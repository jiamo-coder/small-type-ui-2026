# Leego Design UI · Skill Logo 使用标准

```text
Design Standard: Leego Design UI
Standard ID: leego-design-ui
Version: 2.13.0
```

## 1. 核心选择

Leego Design UI 使用蓝色身份点 `#3F67E8`。三条路线来自同一 Skill Logo 家族，但不是三个可以任意轮换的版本：

- **R02 开口框架是主标。** 用于官网导航、Skill 入口、文档页眉与能力总览。
- **R01 圆润构件是轻量变体。** 只用于内容栏目、轻量品牌表达和有名称伴随的社交头像。
- **R03 实心模块是小尺寸变体。** 用于 favicon、Skill 列表、App Icon、系统切换器及深色背景。

## 2. 响应式选型

| 场景 | 首选 | 最小尺寸 | 规则 |
|---|---|---:|---|
| 官网与规范站导航 | R02 | 20px | 符号旁保留真实文字 `Leego Design UI` |
| 文档、能力总览 | R02 | 20px | 不封闭开口，不移动右上身份点 |
| 内容栏目、轻量入口 | R01 | 24px | 必须有名称或上下文伴随 |
| Skill 列表、favicon | R03 | 16px | 不再叠加白色容器和阴影 |
| 深色侧栏或深色底 | R03 | 16px | R02 若必须出现，放入中性浅色容器 |

低于路线最小尺寸时必须切换到 R03，不得通过减细线宽或删除构件勉强缩小。

## 3. 颜色与背景

- UI 身份色为 `#3F67E8`，墨色为 `#191A17`，不得用青黛紫或业务状态色替代身份蓝。
- 浅色背景优先使用 R02 原色；深色背景优先使用 R03。
- 禁止使用 CSS `filter`、描边、渐变、发光或投影制造反白版本。
- 蓝色身份点只表达 Skill 成员身份，不表示信息、选中或成功状态。

## 4. 字标与名称

- 对外名称必须写作 `Leego Design UI`，不得写成 `leego-design-UI`、`Leego Design ui` 或把版本号写进品牌名。
- 数字产品中采用“SVG 符号＋HTML/原生文本”的组合，确保无障碍名称、语言适配和字号缩放。
- SVG 锁定字标尚未完成字体轮廓化，不作为不可变生产资产；印刷、大型广告和商标申请需另行完成字标定稿与法务核验。

## 5. 与其他图标层级的边界

Skill Logo 只标识 Leego Design UI。小葡萄、小红花等产品使用产品 Logo 家族；页面动作使用通用 UI 图标；微信、美团、银行等使用外部生态品牌登记资产。四层标识不得互相替代，也不得用 Skill Logo 表示菜单、状态或业务对象。

## 6. 工程与验收

- 资产从 `skill-logo-family.json` 读取，不在页面中重画 SVG 路径。
- SVG 必须保留 `viewBox="0 0 64 64"`，不得包含脚本、外链、滤镜或位图。
- 导航中图形尺寸为 34px，Skill 列表/图标入口建议 20–32px，favicon 使用 R03。
- 200% 缩放与 320px 宽度下名称不遮挡，纯图标交互热区不低于 44×44px。
- 检查 R02 开口方向、四枚内部模块和右上身份点是否完整。

## 7. 资产状态

本标准批准三条路线用于数字产品，并固定 R02 为主标。当前未完成不可变字标轮廓化与商标检索；这不影响官网和 Skill 的数字界面使用，但不得宣称已完成商标注册或印刷级定稿。
