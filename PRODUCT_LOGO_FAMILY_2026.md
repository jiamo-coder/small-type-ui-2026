# leego-design-UI · 产品 Logo 家族

```text
Design Standard: leego-design-UI
Standard ID: leego-design-ui
Version: 2.12.0
Source Package: system-logo-family/v003
```

## 1. 采用结论

小葡萄、小红花、小算盘、小金矿、小葫芦、小紫薯、小太阳、小点位、元码象、溯源狸和小总管，共享同一业务语义图形、右上身份点、64×64 网格与 3.5 线宽。三条路线不是三套互相竞争的品牌，而是同一产品身份在不同触点中的受控变体。

- `R02 开口框架` 是企业工作台默认母语法。
- `R03 实心模块` 是 App、Pad 桌面、系统切换器和快捷入口的强识别变体。
- `R01 圆润构件` 是员工端、文化、培训和轻量入口的亲和变体。

同一导航、同一系统切换器或同一组并列产品必须使用同一条路线，不能按产品随意混搭。

## 2. 场景选择

| 场景 | 默认路线 | 默认资产 | 说明 |
|---|---|---|---|
| Web 企业后台侧栏品牌区 | R02 | `symbols` 或 `lockups` | 先建立统一的系统框架，再用身份点区分产品。 |
| Web 顶栏、文档页眉、品牌总览 | R02 | `lockups` | 空间不足时退回 `symbols`＋界面文字，不压缩横向组合。 |
| 深色侧栏或深色浮层 | R02 | `mono-light` | 只使用官方反白资产；不得用 CSS `filter` 临时生成。 |
| App Icon、Pad 桌面 | R03 | `app-icons` | 使用实心模块，不另加阴影、底板或外框。 |
| 系统切换器、快捷入口 | R03 | `app-icons` 或 `symbols` | 20–32px 优先 R03；并列项必须保持同一路线和相同尺寸。 |
| 员工端、培训、文化活动 | R01 | `symbols` 或 `app-icons` | 24px 以下不作为无文字的唯一识别入口。 |
| 单色印刷、雕刻、低色数输出 | 当前路线 | `mono-dark` / `mono-light` | 按承载背景选择深色或反白源文件，不自行描边改造。 |

## 3. 尺寸与背景

- R01 图形标建议不低于 24px；更小尺寸必须与产品名称并列。
- R02 图形标建议不低于 20px；16px 只用于有文字标签的紧凑列表，并需做真机清晰度检查。
- R03 图形标建议不低于 20px；App Icon 使用平台要求的完整导出规格，不把 64×64 SVG 直接当商店上传文件。
- 中文横向组合建议宽度不低于 112px；不足时使用图形标与界面文字组合。
- 浅色背景优先使用彩色 `symbols` 或 `app-icons`；深色背景优先使用 `mono-light`，或把彩色 `app-icons` 放在明确的浅色中性容器内。
- 禁止改动比例、身份点位置、线宽、颜色关系、开口方向与外轮廓；禁止渐变、投影、发光和装饰性表情。

## 4. 资产结构

```text
icons/products/
├── r01/
├── r02/
└── r03/
    ├── symbols/<product-id>.svg
    ├── app-icons/<product-id>.svg
    ├── lockups/<product-id>-zh.svg
    ├── mono-dark/<product-id>.svg
    └── mono-light/<product-id>.svg
```

机器可读目录为 `product-logo-family.json`，TypeScript 映射为 `product-logo-family.ts`。兼容路径 `icons/<product-id>.svg` 固定指向 R02 `app-icons`，新代码应优先显式指定路线与资产类型。

## 5. 品牌与 UI 语义边界

- 产品身份色只表达“这是哪个系统”，不得表达成功、关注、危险、食安或完成状态。
- 状态仍读取 `color.semantic`；Logo 附近出现状态时使用独立状态徽标和文字，不给 Logo 改色。
- 中文横向组合保留源包中的可编辑系统字体文本。用于不可变印刷、商标申报或供应商生产前，需要品牌团队提供已轮廓化且完成字体许可确认的生产资产。
- 源包仍记录为 `provisional-three-route-review`；本规范只确认三条路线作为场景变体收录，不替代商标检索、字体授权和品牌法律审查。

## 6. 验收

- 11 个产品、3 条路线、5 类资产完整，共 165 个 SVG，ID 与文件名唯一。
- SVG 不包含远程图片、脚本、滤镜、渐变或嵌入位图。
- 16/20/24/32/48px、浅色/深色背景均有可辨识结果；低于路线最小尺寸时按规则回退。
- 同一并列集合只使用一条路线；深色背景只使用真实反白资产或中性浅色容器。
- 产品身份色与业务状态色相互独立。
