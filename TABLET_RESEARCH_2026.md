# 2026 小字号 UI 2.0 · Pad / Tablet 布局调研

```text
Design Standard: 2026 小字号 UI 2.0
Standard ID: small-type-ui-2026
Version: 2.3.0
```

## 结论

Pad 不是放大的手机，也不是缩小的桌面后台。企业 Pad 的布局必须先识别操作姿态、任务闭环、输入设备和持续使用时间，再决定导航、主工作区与行动区的组合。

“2026 小字号 UI 2.0”将 Pad 设为独立平台层，并采用四种基础模板：概览网格、列表详情、支持面板和高频作业台。视觉继续沿用青黛紫、玉青、朱砂与中性色；参考案例只用于学习空间组织，不复制其配色、图形或业务内容。

## 调研材料

### 官方平台基准

- [Apple Human Interface Guidelines · Sidebars](https://developer.apple.com/design/human-interface-guidelines/sidebars)：大屏可使用侧栏提供稳定导航；层级超过两级时应考虑侧栏、内容列表和详情组成的分栏结构；空间不足时改用更紧凑的导航。
- [Apple Human Interface Guidelines · Layout](https://developer.apple.com/design/human-interface-guidelines/layout)：界面要随窗口尺寸和环境动态适配，重要信息应保持易发现，相关信息通过空间、背景、形状或分隔建立分组。
- [Android Developers · Canonical layouts](https://developer.android.com/develop/adaptive-apps/guides/canonical-layouts)：大屏优先采用 feed、list-detail 和 supporting pane 等经过验证的结构，并根据实际窗口宽度切换单栏、双栏或支持面板。
- [Android Developers · Support different display sizes](https://developer.android.com/develop/adaptive-apps/guides/support-different-display-sizes)：响应式不仅是按比例缩放，还包括在窗口变化时显式替换内容级布局。
- [Material Design 3 · Canonical layout examples](https://m3.material.io/foundations/layout/canonical-examples/overview)：布局脚手架由 bar、rail、pane 组成；紧凑、中等和展开窗口应使用不同组合，购物篮、审核等聚焦任务可使用叠加支持面板。

### 用户提供的布局参考

五张参考图可归纳为三种操作姿态：

1. **浏览与分析**：左侧图标栏保持全局导航，中部使用多栏卡片、日历或图表，右侧提供与当前对象相关的支持内容。
2. **配置与决策**：主对象、配置选项与结果预览集中在主区，订单或结算摘要固定在右侧，减少来回跳转。
3. **高频现场作业**：导航、批次/对象列表、商品或数据网格、数字键盘和主操作在同一屏稳定分区，位置不随任务切换漂移。

这些参考只证明空间结构与任务姿态之间的关系。图片中的颜色、字体、图标、业务名称和装饰语言不进入本规范。

## 设计决策

### 1. 先定任务姿态，再选模板

| 任务特征 | 推荐模板 | 关键结构 |
| --- | --- | --- |
| 扫描多个模块、浏览趋势 | 概览网格 | 导航栏 + 12 列内容网格 + 可选支持面板 |
| 从列表连续查看对象详情 | 列表详情 | 导航栏 + 240–320px 主列表 + 详情区 |
| 配置、审核、结算、复核 | 支持面板 | 主工作区约 70% + 280–360px 固定行动区 |
| 收银、开单、盘点、现场录入 | 高频作业台 | 稳定分区 + 大触控目标 + 固定数字/主操作区 |

### 2. 视口与适配

- 主要验收视口：1024×768、1280×800、834×1194。
- `≥1180px`：允许导航栏、主工作区和支持面板三块同时常驻。
- `960–1179px`：保留导航栏与两块核心内容；非关键支持面板改为侧滑层。
- `760–959px`：按 Pad 竖屏处理，默认单一主工作区加可恢复的侧滑层；不得自动改成手机底部导航。
- `<760px`：进入 Mobile 独立信息架构。
- 旋转、分屏或窗口缩放后必须保留选中对象、筛选、滚动位置、草稿与未提交输入。

### 3. 导航与分栏

- Pad 导航栏默认 76px，紧凑模式 68px；只有宽度达到 1180px 且文字导航能提高效率时，才允许展开到 220px。
- 主列表默认 280px，范围 240–320px；支持面板默认 312px，范围 280–360px。
- 关键提交、结算、确认或处置区可常驻；只读补充信息应在空间不足时收进侧滑层。
- 侧栏最多展示两级信息层级。更深层级通过列表详情或页面下钻承接。

### 4. 触控、输入与操作安全

- 触控目标绝对最小 44×44px，默认 48×48px，高频作业采用 52×52px。
- 默认控件高度 44px，作业控件 48px，固定主操作 56px。
- 高频数字键、商品卡或快捷选择块不得小于 88px 的最短边。
- 不依赖悬停暴露关键功能；同时兼容手指、触控笔、键盘、鼠标和扫码枪等输入。
- 危险动作与主操作保持物理间隔，并提供清晰名称、结果预览与必要确认。
- 站立和双手作业场景应保持主要输入区位置稳定，避免提交后重新排版导致误触。

### 5. 中文排版与密度

- 页面标题 24/32px、区块标题 20/28px、卡片标题 17/24px。
- 正文 15/23px、控件 15/22px、表头 13/20px、辅助和图表标注 12/18px。
- 关键数字 24/32px；所有可见文字不低于 12px。
- 常规行高 56px，高频列表可为 64px；只有键鼠优先的只读密表允许 48px。
- 卡片内边距 14–18px，模块间距 12px，图表高度 160–220px。

### 6. 视觉与状态

- 颜色、圆角、边框、字体和图标读取现有 2.0 令牌，不创建 Pad 专属品牌色。
- 青黛紫用于主操作和选中态；玉青仅用于安全、成功和恢复；朱砂仅用于危险、失败与必须处理。
- 颜色必须与图标、文字、边框或状态面共同编码。
- 加载、空数据、错误、权限不足、过期、离线和设备断开都需要稳定占位，不能导致主要操作区域跳动。

## 禁止做法

- 把 1440px 桌面界面按比例压缩到 1024px。
- 把手机底部导航直接拉宽到 Pad。
- 为了同屏显示更多内容而使用 7–11px 中文文字或小于 44px 的关键触控目标。
- 在所有 Pad 页面固定三栏，不根据任务和实际窗口宽度调整。
- 让关键操作只在 hover、右键或隐藏手势中出现。
- 让右侧支持面板与主区争夺同等视觉权重，或把低频信息永久占据首屏。

## 验收清单

- 三组基准视口和横竖屏切换均无内容遮挡、文字重叠或关键功能丢失。
- 手指、键盘、鼠标和触控笔均能完成主要任务；焦点顺序与视觉顺序一致。
- 用户从选中对象到完成主要动作不需要跨页面反复确认上下文。
- 固定行动区显示当前对象、结果、风险与提交反馈，不产生“操作对象不明确”。
- 200% 字体/浏览器缩放、长中文、长金额、负值、未知值下保持可用。
- 离线、同步失败、外设断开和权限不足时保留草稿或上次有效状态，并给出恢复动作。
