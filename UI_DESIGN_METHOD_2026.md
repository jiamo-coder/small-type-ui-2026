# Leego Design UI · 设计问诊与减法方法

```text
Design Standard: Leego Design UI
Standard ID: leego-design-ui
Version: 2.12.0
```

本方法用于新页面设计、已有系统审计和已确认范围的前端实施。目标是先消除会改变方案的未知，再用固定模板和减法检查减少随机性。它不替代业务确认，也不授权修改后台接口、权限、数据结构或业务规则。

## 1. 三种工作模式

### 新设计

先读取用户给出的素材、业务文档和现有系统事实。输出 `Design Read`，经用户确认后再交付页面结构、交互说明、视觉方案或原型。

### 已有系统审计

默认只读。生成 `UI_2_AUDIT.md`，用户确认页面和 P0/P1 范围后才允许修改产品代码。

### 已确认实施

只实施审计报告中已确认的范围。保留业务逻辑、接口、权限、数据和技术栈；优先使用现有组件并统一令牌。

## 2. 自适应需求问诊

先从仓库、页面、截图和文档中查找事实，不询问可以自行发现的内容。

- 信息已经覆盖主要用户、首要任务、平台、对象与状态、关键操作、保护范围和成功标准时，不额外提问。
- 缺少会改变结构、权限、范围或验收的决定时，每轮询问 1–3 个关键问题，并给出推荐答案和影响。
- 只有全新系统、目标明显模糊或用户明确要求深度访谈时，才进入多轮问答。
- 视觉偏好不能替代业务目标；参考图只能证明层级、比例或交互偏好，不能授权复制品牌资产、文案和业务数据。

### UI-ready 判定

以下内容不存在会改变核心方案的未知时，可以结束问诊：

1. 主要用户与使用环境。
2. 页面必须帮助用户完成的首要判断或行动。
3. Web、Mobile、Tablet 或 Website 平台。
4. 主要对象、数据来源、状态、异常与数据新鲜度。
5. 可执行操作、权限和不可逆风险。
6. 不可修改的业务能力、接口与已有组件。
7. 可验证的成功标准。

## 3. Design Read

进入设计前使用以下紧凑结构，不写成长篇方案：

```text
模式：新设计 / 审计 / 实施
平台：Web / Mobile / Tablet / Website
主要用户：谁在什么环境使用
首要任务：首屏必须帮助他判断或完成什么
主模板：一个主模板，可选一个支持模板
保护内容：不能删除或改写的业务信息与能力
减法目标：当前准备删除、合并或降级的重复内容
开放决策：仍需用户确认且会改变方案的事项
成功标准：可观察、可测试的结果
```

新设计需要用户确认 Design Read；已有系统把同样信息写入审计报告，不以聊天确认替代审计范围。

## 4. 确定性模板选择

按固定顺序选型：

```text
平台 → 页面目标 → 对象规模 → 操作频率 → 证据/审核要求 → 主模板＋最多一个支持模板
```

- 主模板决定页面阅读顺序和交互闭环。
- 支持模板只能补充局部能力，不得把两个完整工作台堆在同一页。
- 业务名称、字段、接口和权限映射到模板，不复制来源系统规则。
- 企业后台、Mobile 和 Tablet 使用固定产品框架；Website 可替换品牌色、标题气质、主视觉和图片风格，但不能降低真实性、转化、响应式和可访问性标准。

## 5. 减法门禁

每个可见元素至少服务于一种目的：

`页面上下文 / 用户决策 / 业务行动 / 状态风险 / 数据证据 / 可访问性`

无法归入任何目的时，优先删除；仍有局部价值时合并或降级。

### 六类重复

1. **身份重复**：固定顶栏、面包屑、页面标题、英文眉题和介绍段重复表达页面身份。
2. **数据重复**：同一 KPI 在指标卡、图表标题和结论段重复展示，且没有新增比较或证据。
3. **操作重复**：多个全局搜索、重复 CTA、相同筛选或导出入口。
4. **容器重复**：卡片套卡片、重复边框、没有层级作用的背景与阴影。
5. **说明重复**：界面已经清楚表达的操作再写成介绍文字。
6. **装饰重复**：无语义图标、编号、标签、强调色和动效。

### 不得误删

对象身份、范围与时间、风险结论、数据新鲜度、权限上下文、证据来源、错误恢复、必要字段帮助、法律与安全信息不能因追求简洁而隐藏。

## 6. 审计表达

每项发现按以下顺序记录：

```text
屏幕上看到什么 → 给用户造成什么成本 → 删除或调整什么 → 保护哪些内容
```

`UI_2_AUDIT.md` 至少包含：

- 需求完整度与 Design Read。
- 模板映射。
- 减法机会。
- 固定素材一致性：字体、图标、侧栏、顶栏、内容间距和动效。
- 受保护内容。
- P0/P1/P2、迁移风险和验收方法。

## 7. 固定产品标准

- Web 侧栏：216px 展开、68px 折叠、248px 移动抽屉。
- Web 工作顶栏：64px，顺序为面包屑、搜索、通知、身份、退出。
- Web 内容：左 28px、右 16px、首模块和模块间距 12px。
- Web 正文 14/22px；Mobile 正文 16/24px；所有可见文字不低于 12px。
- 字重只使用 400、500、600、700。
- 通用图标使用 24×24 网格和 1.75px 圆角线条；导航 20px，顶栏 18px，纯图标按钮热区至少 44px。
- 组件规范覆盖 Default、Hover、Focus-visible、Active、Disabled、Loading、Error、Success。

精确令牌以 `tokens.json` 为唯一事实来源。不得通过项目局部样式改写固定外壳尺寸。

## 8. 交付前检查

1. 真实内容、长中文、零值、负值、未知、过期、加载、空、错误和权限不足均可稳定呈现。
2. 所有启用控件都有真实行为或明确原型反馈。
3. 关键任务可用键盘完成，焦点清楚，纯图标操作有可访问名称。
4. Web 检查 200% 缩放；Mobile/Tablet 检查系统字号和运行时窗口变化。
5. 不引入未经确认的接口、第三方依赖、远程字体、遥测或虚假数据。
6. 运行字体、UI 质量、Lint、生产构建及与范围匹配的响应式检查。

