# 2026 小字号 UI 2.0 · 企业产品动效系统

```text
Design Standard: 2026 小字号 UI 2.0
Standard ID: small-type-ui-2026
Version: 2.11.0
```

本规范用于企业 Web 工作台、Mobile App、Pad 作业台和数据可视化界面。动效的目标是让状态、空间关系和操作结果更容易理解，不是让高频界面持续表演。

## 1. 决策顺序

任何动效都按以下顺序判断：

1. **频率**：高频操作优先即时响应；偶发任务才使用标准动效；稀有成功或首次引导才允许强调感。
2. **目的**：只能是反馈、空间连续性、状态指示、防止突变、解释，或稀有场景的愉悦感。
3. **工具**：Web 优先 CSS transition、`@starting-style` 和 WAAPI；Mobile/Pad 优先平台原生导航与 UI 线程手势能力。
4. **属性**：默认只改变 `transform` 和 `opacity`。结构性侧栏和折叠内容是受控例外，必须限制范围并检查性能。
5. **中断与退出**：用户快速重复操作时，动效从当前视觉状态继续，不从第一帧重启；退出方向和来源保持一致。
6. **降级**：减少动态效果时移除位移、缩放、视差和过冲，但保留必要的颜色、透明度和状态反馈。

不能说明目的，或数据正在被用户读取和操作时，默认不增加装饰动效。

## 2. 使用频率

| 频率 | 处理方式 |
|---|---|
| 每日上百次、键盘快捷键、核心导航 | 即时切换，不播放入场动画 |
| 每日数十次、表格操作、常用筛选 | 0–160ms，近乎不可感知或不动 |
| 偶发弹窗、抽屉、Toast、状态切换 | 160–240ms 标准动效 |
| 首次引导、稀有完成与庆祝 | 最长 280ms，可使用克制强调 |

动效不得阻止点击、键盘操作、焦点移动或数据更新。

## 3. 令牌

唯一来源为 `tokens.json.motion`：

- 时长：`instant 0`、`press 120`、`tooltip 140`、`fast 160`、`popover 180`、`standard 200`、`shell 220`、`modal 240`、`emphasis 280`。
- 曲线：进入/退出使用 `enter`，屏内位移使用 `move`，抽屉使用 `drawer`，确定速率的进度使用 `constant`。
- 形变：按压 `0.97`，入场缩放不得低于 `0.96`；禁止 `scale(0)`。
- 错峰：每项 40ms，最多五项，总延迟不超过 160ms；表格和虚拟列表禁止错峰入场。

禁止 `transition: all`、未登记的时长和曲线，以及通过 `left`、`top`、`margin`、`padding` 做常规运动。

## 4. Web

- 高频页面导航、`⌘K / Ctrl+K`、侧栏菜单切换和表格行操作不做整页入场。
- 独立主要按钮可以提供 120ms 按压反馈；密集工具栏和表格操作不统一缩放。
- 浮层从触发点出现；模态框保持视口中心；抽屉沿其所在边缘进入和退出。
- 侧栏宽度和折叠内容属于结构性布局例外，只能使用登记令牌并在目标设备检查布局与绘制开销。
- Hover 动效只在 `(hover: hover) and (pointer: fine)` 生效，键盘焦点必须拥有等价的静态反馈。
- 不为简单淡入安装 Motion、GSAP 或其他第三方依赖；项目已有动效库时才复用。

## 5. Mobile

- Tab 是并列关系，页面内容不左右滑动；选中指示可以短促移动。
- 层级导航、模态和系统 Sheet 优先使用平台原生过渡，不重建一套 JS 页面转场。
- 手势必须可中断、继承释放速度，并避免逐帧触发 React 状态更新。
- 触觉反馈每次用户提交最多一次，必须与视觉状态同帧出现，不能成为唯一反馈。
- 在系统字号 200%、减少动态效果和较慢的实际设备发布构建中验证。
- 不因为规范存在而自动安装 Expo、Reanimated、Gesture Handler 或触觉依赖。

## 6. Pad / Tablet

- 高频录入、收银、盘点和审核作业以即时响应为主，不给每个输入、行选择或步骤切换增加动画。
- 分栏展开、支持面板和对象切换仅在帮助理解空间关系时使用短动效。
- 窗口尺寸、横竖屏和分屏变化必须保持选中对象、草稿、焦点与滚动位置；不能用转场掩盖状态丢失。
- 触控、键盘、鼠标和外设输入获得一致的状态结果，Hover 不能承载唯一操作。

## 7. 数据可视化

- 初次加载不逐线描绘，不让指标从零滚动到真实值。
- 筛选或时间范围变化时，只对保持同一身份的图形对象做 160ms 内过渡；单位、基线和更新时间立即更新。
- 坐标范围变化必须明确，不能通过平滑动画掩盖比例跳变。
- 实时数据和高危预警立即出现，绝不等待动画完成。
- 高危状态不循环闪烁；使用危险色、图标、文字、边框和状态面持续表达。
- 大表格、排行榜和虚拟列表不逐行入场，不因滚动重复播放。
- 骨架只表示真实加载，不用无限高亮制造忙碌感；减少动态效果时使用静态骨架。

## 8. 无障碍与性能

- `prefers-reduced-motion` 不是删除所有反馈。保留短透明度和颜色变化，移除位移、缩放、视差、摇晃和弹跳。
- 任何动效前后都必须保持可读文字、可见焦点、正确语义和稳定触控目标。
- 快速重复操作必须可中断；用户不需要等动画结束才能继续任务。
- Web 使用 DevTools 慢速和性能面板检查；Mobile/Pad 使用最慢支持设备的发布构建检查。
- 发现掉帧时优先删除或缩短动效，再考虑替换工具，不以新增依赖掩盖结构问题。

## 9. 审计分级

- **P0**：动效阻断任务或数据更新；忽略减少动态效果；高危预警被延迟；高频/键盘操作强制转场；手势导致状态丢失；动效新增未授权接口或依赖。
- **P1**：使用未登记时长/曲线；`transition: all`；不可中断；错误来源方向；布局属性造成明显抖动；表格或图表装饰动效妨碍读取。
- **P2**：轻微时长、距离、错峰和视觉一致性问题，不影响任务完成。

审计报告必须记录频率、目的、触发方式、属性、令牌、可中断性、减少动态效果、性能验证和可观察验收。

## 10. 来源与原创边界

本规范研究参考了 Emil Kowalski 的 MIT 许可设计工程 Skill，以及 Web、Apple、Android 与 React Native 的平台动效原则。这里只吸收方法论和公开技术事实，使用本规范自己的中文结构、企业场景、令牌和验收规则；不复制其品牌表达，也不要求安装其推荐的第三方库。

参考来源：<https://github.com/emilkowalski/skills>
