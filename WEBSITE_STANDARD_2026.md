# Leego Design UI · 企业官网标准

Design Standard: Leego Design UI  
Standard ID: leego-design-ui  
Version: 2.13.0

## 1. 适用边界

本标准适用于企业官网、产品官网、能力官网及其落地页。官网是连续阅读与转化界面，不继承企业工作台的 216px 侧栏、64px 工作顶栏、后台卡片密度或多任务操作外壳。

Goodthings 官网是第一份只读设计来源：借鉴其连续叙事、编辑式节奏、单一主转化与真实内容边界，不复制品牌、图片、业务承诺或公司信息。

## 2. 固定叙事结构

默认顺序为：

`品牌价值 → 用户问题 → 解决方案 → 产品/能力 → 证据 → 交付方式 → 信任与边界 → 联系转化`

页面可以删减中间章节，但首屏价值主张、能力证明、真实证据与主要转化不可缺失。每个章节只承担一个阅读问题；不使用等宽卡片铺满整页，通过观点标题、分栏、线性列表、架构图、图片证据及深浅区块建立阅读节奏。

## 3. 导航与滚动

- 页面使用 `body` 单一连续滚动，禁止内部整页滚动容器、滚动劫持和无目的视差。
- 桌面导航由品牌、4–6 个核心入口和一个主 CTA 组成；高度 72px。
- 移动导航高度 64px，显示品牌和菜单按钮；抽屉支持遮罩、`Esc`、导航后关闭和焦点回收。
- 固定导航跳转锚点时设置正确的 `scroll-margin-top`，不遮挡章节标题。
- 提供“跳到正文”入口；全站保持键盘焦点清晰可见。

## 4. 转化与真实性

- 每个页面只有一个主要转化目标；同一视口最多显示一个主按钮和一个次按钮。
- CTA 文案说明动作与结果，不使用多个同权重入口争抢注意力。
- 没有真实表单、二维码、电话或联系方式时显示“联系入口待配置”并禁用提交，不制造假成功。
- 客户 Logo、数据成果、评价、合作关系、认证和案例必须可追溯且获得授权；未确认时使用“内容待核实”结构占位。
- 结构化数据只能声明页面真实展示的组织、产品、文章或案例事实。

## 5. 令牌

### 外壳

- 内容最大宽度：1440px；长文阅读宽度：720px。
- 页面边距：桌面 40px、紧凑桌面 32px、移动 20px。
- 章节间距：桌面 96px、紧凑桌面 72px、移动 56px。
- 主操作最小高度 48px；所有触控目标不低于 44×44px。

### 中文排版

- 桌面首屏标题：44–72px / 1.1；移动首屏标题：36–44px / 1.16。
- 桌面章节标题：38–64px；移动章节标题：30–36px。
- 导语：17/30px；正文：15/27px；元信息不得低于 12px。
- 字重只使用 400、500、600、700。正文使用系统中文无衬线字体；标题可用项目已授权的本地衬线或无衬线字体。
- 禁止远程字体下载、用 8–11px 文字制造“精致感”、用 550/650 等不稳定字重。

## 6. 图片、动效与性能

- 图片和媒体必须声明宽高或 `aspect-ratio`，避免布局偏移；首屏重点图片优先加载，折叠线以下图片延迟加载。
- 不自动播放有声视频，不让视频、粒子或装饰动画替代价值主张。
- 动效只用于导航反馈、轻量悬停、必要的空间连续性和低频品牌表达；遵循现有 Motion 令牌。
- `prefers-reduced-motion` 下关闭位移、漂移、视差和缩放，仅保留必要的颜色或透明度反馈。
- 上线目标：LCP ≤ 2.5s、INP ≤ 200ms、CLS ≤ 0.1；不因验收自动增加遥测或第三方统计。

## 7. 响应式与无障碍

- 在 1440、1200、1024、760、390 和 320 CSS px 下保证重排，不通过水平缩放桌面页面适配移动端。
- 支持浏览器 200% 缩放，无文字重叠、操作遮挡或功能丢失。
- 正文、按钮、链接、表单和非文字控件达到 WCAG 2.2 AA 的对比度与键盘操作要求。
- 使用唯一 H1、连续标题层级、语义化地标、描述性链接文本和有效替代文本。
- 图片缺失时保留版面与说明；无 JavaScript 时仍能阅读核心叙事和联系方式。

## 8. SEO 与发布环境

- 原型和预发布环境默认 `noindex`；只有生产环境输出生产域名 canonical、robots、sitemap 和 Open Graph。
- 每页使用唯一标题与描述；404 返回明确状态并提供恢复路径。
- 生产发布前核实域名、备案信息、公安联网备案、隐私政策、联系方式、Cookie/数据采集说明的适用性。Skill 不编造备案号，不替代法律判断。

## 9. 审计清单

官网审计至少检查：价值主张、目标受众、证据真实性、主 CTA、导航层级、连续滚动、内容节奏、图片授权、标题结构、SEO 元数据、Core Web Vitals、320px 重排、200% 缩放、键盘焦点、联系入口和中国境内发布信息。

以下问题列为 P0：把后台应用壳套到官网、虚构社会证明、多个主 CTA 争抢、假表单或假联系入口、移动端无法完成主要转化、生产元数据指向错误域名、核心内容不可无障碍读取。局部间距、字阶和装饰动效偏差列为 P1。

## 10. 实施边界

实施模式只调整已确认范围内的前端结构、语义、样式和真实状态映射。不得自行新增表单后端、数据库、统计脚本、客户数据、第三方依赖、远程字体或未授权品牌资产。
