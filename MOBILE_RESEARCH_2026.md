# 2026 小字号 UI 2.0 · 中文移动数据界面研究

```text
Design Standard: 2026 小字号 UI 2.0
Standard ID: small-type-ui-2026
Version: 2.8.0
```

## 研究范围

本次研究基于用户提供的四张中文企业经营 App 截图，仅提炼移动端的信息架构、阅读顺序和交互模式。示例中的品牌、行业文案、绿色主色、图标造型和装饰效果不进入规范；视觉实现继续读取“2026 小字号 UI 2.0”的青黛紫、玉青、朱砂与中性色令牌。

## 可复用的四类布局

### 1. 经营摘要看板

适用于管理者快速查看一个组织、区域或业务范围的经营状态。

- 页面顺序固定为：范围上下文 → 时间范围 → 核心指标 → 多指标矩阵 → 趋势摘要。
- 核心指标同时呈现当前值、上期和同期，但涨跌颜色必须由指标语义决定，不能默认“上涨红、下降绿”。
- 2×2 指标矩阵用于扫描，每个指标都可进入单指标详情。
- 数据更新时间靠近范围与核心指标，过期或待同步状态显式显示。

### 2. 分类报表门户

适用于报表数量较多、需要按目标快速找到入口的系统。

- 报表按经营、项目、效率等用户目标分组，不按后台菜单或数据表名称分组。
- 每个入口由 38–44px 类别图标、14px 标题和 12–13px 一句说明组成。
- 双列入口只用于标题与说明都较短的场景；长标题或存在状态信息时切换为单列。
- 公告或月报卡最多保留一张，不能把功能入口推离首屏。

### 3. 单指标分析

适用于解释一个关键指标的数值、构成、趋势和来源。

- 首屏先给范围、时间粒度、指标选择和更新时间，再展示数字。
- 指标存在计算关系时，用紧凑等式展示构成，并提供口径说明入口。
- 图表与列表是同一数据的两种视图，切换后保留范围、指标和时间粒度。
- 图表高度 120–180px，至少显示当期与一个比较基线，并提供一句文字结论。
- 前后日期或周期切换放在图表底部，返回上一页时保留原筛选与滚动位置。

### 4. 移动排行表

适用于对象排名、三项以内的关键指标比较。

- 不把桌面宽表等比缩小。每行先展示排名与完整对象名，再以三列数据组呈现关键值。
- 排序字段通过表头文字、方向符号和选中颜色共同表达。
- 对象名称允许单行截断，但整行可进入详情并提供完整名称。
- 超过三个关键指标时，保留主指标，其余进入详情或指标切换器。
- 排名变化、负值、零值、未知值和过期状态必须保持列对齐。

## 中文移动信息密度

- 页面标题 24/32px，卡片标题 17/24px，正文 16/24px，控件 15/22px，辅助文字 13/20px，图表与表头最低 12/18px。
- 核心经营数字可使用 32–40px，但要为长金额、小数、负值和单位预留稳定空间。
- 卡片内边距 12–16px，列表行 60–84px，所有触控目标不低于 44×44px。
- 密度通过分组、对齐、短说明和下钻获得，不通过 7–11px 中文文字获得。

## 通用化边界

- 经营、组织、项目和指标只作为通用示例，不自动引入门店、食安、店长等行业语义。
- 品牌色只表达身份和主操作；成功、恢复用玉青，危险和失败用朱砂，普通下降使用风险橙或中性文字。
- 参考图中的系统状态栏、品牌 Logo、水印、专有底部导航和营销插画不属于模板资产。

## 验收清单

- 390×844 与 430×932 下核心指标、长数字和中文标题不重叠。
- 15 类移动模板均可从选择器打开；经营概览可下钻指标详情，报表门户可进入趋势或排行。
- 筛选、页签、图表/列表切换、排序和周期切换都有可观察反馈。
- 图表标注范围、基线和更新时间；排行行最多三项关键指标。
- 系统字号放大后不依赖极小文字维持布局，触控区仍不低于 44px。
